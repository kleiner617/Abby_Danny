type OurStoryProps = {
	className?: string;
	id?: string;
	isMobile?: boolean;
};
const OurStoryContainer: React.FC<OurStoryProps> = (props: OurStoryProps) => {
	return null;
};

export default OurStoryContainer;
